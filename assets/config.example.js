// Copy to `assets/config.js` and fill in values.
window.UTCH_CONFIG = {
  // Google Calendar embed URL (Settings and sharing → Integrate calendar → Embed code src)
  calendarEmbedUrl: "",
  // Optional public ICS URL (Settings and sharing → Integrate calendar → Secret address in iCal format)
  calendarIcsUrl: "",
  // Apps Script Web App URL (Deploy → Manage deployments → Web app)
  appsScriptWebAppUrl: ""
};
